training_stage: exclusive_training

training_param:
  batch_size : 64
  learning_rate : 0.0001
  device: cuda

model_param:
  img_size : 28
  channels : 3
  shared_dim : 64
  exclusive_dim: 8
  switched : True
  
pretrained_encoder:
  enc_x_path :
  enc_y_path : 

loss_param:
  local_mutual_loss_coeff: 1
  global_mutual_loss_coeff: 0.5
  disentangling_loss_coeff: 0.1